name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # Checkout the code from the repository
      - name: Checkout code
        uses: actions/checkout@v3

      # Set up Docker
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Cache Docker layers
        uses: actions/cache@v3
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ github.sha }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      # Build the Docker image
      - name: Build Docker image
        run: docker-compose -f docker-compose.yml build

      # Run tests
      - name: Run tests
        run: docker-compose run --rm app php artisan test

  deploy:
    runs-on: ubuntu-latest
    needs: build
    if: github.ref == 'refs/heads/main'

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Log in to DockerHub
        uses: docker/login-action@v2
        with:
          username: rafi021
          password: mahmud@012143003

      # Push the Docker image to DockerHub
      - name: Push Docker image
        run: |
          docker-compose -f docker-compose.yml build
          docker tag laravel_app:latest rafi021/laravel_app:latest
          docker push rafi021/laravel_app:latest

      # Deploy the application on your server (using SSH)
      # - name: Deploy to Server
      #   env:
      #     SSH_PRIVATE_KEY: 5|Z<6^l%1£:16&^q_!Gzn-47
      #     SERVER_HOST: 134.209.145.61
      #     SERVER_USER: root
      #   run: |
      #     echo "$SSH_PRIVATE_KEY" > id_rsa
      #     chmod 600 id_rsa
      #     ssh -o StrictHostKeyChecking=no -i id_rsa $SERVER_USER@$SERVER_HOST << 'EOF'
      #       docker pull rafi/laravel_app:latest
      #       docker-compose -f /path/to/your/docker-compose.yml down
      #       docker-compose -f /path/to/your/docker-compose.yml up -d
      #     EOF
